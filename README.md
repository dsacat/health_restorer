# Health Restorer

`Health Restorer` — одноразовый сценарий глубокого обслуживания Windows 10/11. Он запрашивает права администратора, создаёт временные задания от имени `SYSTEM`, продолжает работу после перезагрузок и удаляет задания после завершения.

## Готовый EXE

```text
dist/HealthRestorer.exe
```

Контрольная сумма:

```text
dist/HealthRestorer.exe.sha256
```

EXE автономный и однофайловый. Отдельно устанавливать .NET и вручную размещать PowerShell-скрипты не требуется.

## Данные внутри EXE

В свойствах файла записываются:

- автор: `dsa_cat`;
- компания: `dsa_cat_studio`;
- продукт: `Health Restorer`;
- описание: `Health Restorer — автор dsa_cat`;
- копирайт: `Copyright © 2026 dsa_cat`;
- версия продукта: `1.2.0`;
- версия файла: `1.2.0.0`;
- репозиторий: `https://github.com/dsacat/health_restorer`.

В EXE встроена некоммерческая лицензия.

## Порядок работы

1. Обновляются базы Microsoft Defender.
2. Запускается Microsoft Defender Offline до обычной загрузки Windows.
3. После загрузки обязательно запускается отдельный Microsoft Defender Full Scan с ожиданием завершения.
4. На время полной проверки программа пытается включить сканирование архивов, почтовых хранилищ, скриптов, точек восстановления, подключённых съёмных накопителей, сетевых дисков и доступных сетевых файлов.
5. Пользовательские исключения Defender для путей, расширений и процессов временно снимаются и затем восстанавливаются.
6. Выполняется расширенный аудит автозагрузки, включая перенаправленные и скрытые папки Startup, Run/RunOnce, Policies, Winlogon, AppInit, IFEO, Active Setup, Shell hooks, задания Планировщика, автоматические службы, загрузочные драйверы и постоянные WMI-подписки.
7. Создаётся резервная копия и отключаются безопасные пользовательские точки автозагрузки: Run/RunOnce для компьютера и всех локальных пользователей, обычные пользовательские и общие папки Startup, а также сторонние задания входа и загрузки.
8. Критические механизмы вроде Winlogon, системных драйверов, служб Microsoft и WMI-подписок только фиксируются в отчёте и не отключаются вслепую, чтобы не сделать Windows незагружаемой.
9. Выполняется предзагрузочная проверка системного диска.
10. DISM восстанавливает хранилище компонентов Windows.
11. SFC проверяет и восстанавливает защищённые системные файлы.
12. `CHKDSK /scan` проверяет доступные локальные диски, а Defender Full Scan проверяет все доступные смонтированные локальные, съёмные и разрешённые сетевые тома.
13. Очищаются временные файлы, кэш Windows Update, отчёты об ошибках, пользовательский кэш, кэши браузеров, шейдерный кэш, DNS-кэш, кэш эскизов и корзина.
14. Удаляются подтверждённые остатки не до конца удалённых программ: сломанные записи деинсталляции, сторонние службы и задания с отсутствующими файлами, битые ярлыки, пустые и подтверждённо оставшиеся каталоги программ.
15. Локальные накопители оптимизируются через `defrag /O`, который выбирает подходящую операцию для HDD или SSD.
16. После основного обслуживания очищаются остатки ранее удалённых файлов на всех доступных внутренних и съёмных томах с буквами дисков:
    - на NTFS-HDD используется `cipher /w`;
    - на HDD с другой файловой системой свободное пространство заполняется временным нулевым файлом и затем освобождается;
    - на SSD и флеш-памяти выполняется `TRIM/ReTrim`, где он поддерживается.
17. Создаются журнал, отчёт полной проверки, отчёт по автозагрузке и итоговый отчёт.

## Что означает «полная проверка всех файлов»

Microsoft Defender Full Scan запускается через:

```text
MpCmdRun.exe -Scan -ScanType 2 -Timeout 30
```

Программа ждёт завершения процесса. Проверяются все файлы, которые доступны Windows на смонтированных локальных томах и, при разрешённых настройках Defender, на подключённых съёмных и сетевых накопителях.

Невозможно проверить данные, физически недоступные операционной системе: отключённые диски, не загруженные локально облачные файлы, повреждённые нечитаемые области, заблокированные BitLocker-тома, некоторые зашифрованные контейнеры и архивы с неизвестным паролем.

Отчёт полной проверки:

```text
C:\ProgramData\HealthRestorer\defender-full-scan-report.txt
```

Сохранённые настройки Defender:

```text
C:\ProgramData\HealthRestorer\defender-preferences-before-full-scan.json
```

## Охват автозагрузки

Расширенный аудит сохраняется в:

```text
C:\ProgramData\HealthRestorer\autorun-audit.json
```

Он включает:

- стандартные и перенаправленные общие и пользовательские папки Startup;
- `Win32_StartupCommand`;
- машинные и пользовательские Run/RunOnce и Policies Run;
- Winlogon, AppInit DLL, AppCert DLL, IFEO, SilentProcessExit и Active Setup;
- ShellServiceObjectDelayLoad и ShellExecuteHooks;
- задания входа, загрузки, регистрации и смены пользовательской сессии;
- автоматические службы, загрузочные и системные драйверы;
- постоянные WMI-фильтры, потребители и привязки.

Абсолютно любой возможный способ закрепления программы перечислить невозможно: вредоносный код может использовать уязвимости, изменённые загрузчики, прошивки или новые техники. Поэтому программа сочетает Defender Offline, Full Scan и широкий аудит, но не обещает невозможную стопроцентную гарантию.

## Очистка ранее удалённых файлов

Сводка по каждому доступному тому сохраняется в:

```text
C:\ProgramData\HealthRestorer\deleted-data-summary.json
```

На HDD перезапись свободного пространства удаляет обычные остатки файлов, которые были удалены из корзины или удалены напрямую, но ещё оставались в неиспользуемых секторах файловой системы.

На SSD и флеш-памяти выполняется TRIM/ReTrim. Из-за контроллера, резервных ячеек и выравнивания износа обычная программа не может гарантировать физическую перезапись каждого NAND-блока. Для такой гарантии нужен Secure Erase/Sanitize всего накопителя, который уничтожает также Windows и все существующие файлы.

Нельзя очистить отключённые, несмонтированные, заблокированные, повреждённые, доступные только для чтения или нераспознанные тома.

## Защита при очистке программных остатков

Сценарий не удаляет произвольную папку только из-за неизвестного названия. Остаток удаляется лишь при сочетании нескольких признаков: отсутствует деинсталлятор, запись не относится к Windows Installer или Microsoft, а в каталоге отсутствуют работающие исполняемые компоненты.

Перед изменением сохраняются экспорт разделов реестра, XML заданий Планировщика, разделы реестра служб, копии битых ярлыков и JSON-отчёт.

Резервная копия метаданных:

```text
C:\ProgramData\HealthRestorer\ProgramResidueBackup-ГГГГММДД-ЧЧММСС
```

## Запуск

1. Сохрани открытые файлы.
2. Создай резервную копию важных данных.
3. Подключи компьютер к питанию.
4. Подключи все накопители, которые нужно проверить и очистить.
5. Запусти `HealthRestorer.exe`.
6. Подтверди запрос прав администратора.

Компьютер перезагрузится несколько раз. Полная проверка, проверка дисков и очистка свободного пространства могут продолжаться много часов.

## Журналы и отчёты

Основной журнал:

```text
C:\ProgramData\HealthRestorer\health-restorer.log
```

Итоговый отчёт:

```text
C:\Users\Public\Desktop\HealthRestorer-report.txt
```

Резервная копия отключённой автозагрузки:

```text
C:\ProgramData\HealthRestorer\StartupBackup-ГГГГММДД-ЧЧММСС
```

## Восстановление автозагрузки

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\HealthRestorer.ps1 -Mode RestoreStartup
```

## Файлы проекта

- `HealthRestorer.ps1` — Defender Offline, обязательный Full Scan, DISM, SFC, диски, кэш и оптимизация.
- `AutorunAudit.ps1` — широкий аудит обычной и скрытой автозагрузки.
- `ProgramResidueCleanup.ps1` — консервативная очистка остатков удалённых программ.
- `SecureDeletedData.ps1` — очистка программных остатков и свободного пространства на доступных томах.
- `Start-HealthRestorer.ps1` — единая точка запуска и регистрация завершающего задания.
- `src/HealthRestorer.Launcher/` — исходный код EXE-запускателя.
- `.github/workflows/build-exe.yml` — проверка синтаксиса, обязательных этапов, метаданных и сборка EXE на Windows runner.

## Ограничения

- Для антивирусных этапов требуется активный Microsoft Defender.
- Tamper Protection или корпоративная политика могут запретить изменение отдельных настроек и исключений; такие случаи фиксируются в журнале.
- DISM может использовать Windows Update.
- Открытые системой файлы кэша иногда невозможно удалить до следующей перезагрузки.
- Неизвестный тип накопителя не перезаписывается вслепую, чтобы не изнашивать возможный SSD; программа пытается выполнить ReTrim и записывает результат.
- Сценарий не заменяет резервное копирование и не исправляет физически неисправный накопитель.

## Лицензия

`Health Restorer Non-Commercial License 1.1` разрешает некоммерческое использование, изучение, изменение и распространение. Коммерческое использование запрещено без отдельного письменного разрешения `dsa_cat`.

Copyright © 2026 dsa_cat.
